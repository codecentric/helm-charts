version: 2
jobs:
  dummy:
    docker:
      - image: busybox
    steps:
      - run:
          command: echo "Dummy job to avoid build errors until we finally disable CircleCI"

workflows:
  version: 2
  dummy:
    jobs:
      - dummy
